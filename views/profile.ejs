<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= member.name %> - Profile</title>
    <style>
        body { font-family: 'Georgia', serif; background-color: #f4ece1; color: #5d4037; display: flex; flex-direction: column; align-items: center; padding: 50px; }
        .profile-container { background: #fffaf4; padding: 40px; border-radius: 8px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); max-width: 800px; width: 100%; text-align: center; }
        .header-section { display: flex; align-items: center; justify-content: center; gap: 40px; margin-bottom: 30px; border-bottom: 1px solid #dcd3c9; padding-bottom: 30px; }
        .main-photo { width: 200px; height: 200px; border-radius: 50%; border: 5px solid #8d6e63; object-fit: cover; }
        .bio-info { text-align: left; }
        .bio-info h1 { margin: 0; font-variant: small-caps; font-size: 2.5em; }
        .album-grid { display: flex; justify-content: center; gap: 20px; flex-wrap: wrap; margin-top: 20px; }
        .album-item { display: flex; flex-direction: column; align-items: center; width: 120px; }
        .album-photo { width: 100px; height: 100px; border-radius: 50%; border: 3px solid #8d6e63; object-fit: cover; }
        .caption { font-size: 0.8em; margin-top: 8px; font-style: italic; }
        .back-btn { margin-top: 30px; text-decoration: none; color: #8d6e63; font-weight: bold; display: block; }
        
        /* Styles for the Poem Link Box */
        .special-record-link { 
            margin-top: 30px; 
            padding: 20px; 
            background: #f9f3e6; 
            border-left: 5px solid #8b5a2b; 
            text-align: left; 
        }
        .btn { 
            display: inline-block; 
            padding: 10px 20px; 
            background: #8b5a2b; 
            color: white; 
            text-decoration: none; 
            border-radius: 5px; 
            margin-top: 10px;
            font-weight: bold;
        }

        /* Modal Styles */
        .modal { display: none; position: fixed; z-index: 1000; left: 0; top: 0; width: 100%; height: 100%; background-color: rgba(0,0,0,0.9); }
        .modal-content-container { margin: auto; display: block; width: 80%; max-width: 600px; aspect-ratio: 1 / 1; position: relative; top: 50%; transform: translateY(-50%); }
        .modal-content { width: 100%; height: 100%; object-fit: cover; border: 5px solid white; }
        .close { position: absolute; top: 20px; right: 35px; color: #f1f1f1; font-size: 40px; font-weight: bold; cursor: pointer; }
        #caption { margin: auto; display: block; width: 80%; text-align: center; color: #ccc; padding: 10px 0; }
    </style>
</head>
<body>
    <div class="profile-container">
        <div class="header-section">
            <img src="/familytree/<%= member.mainImg %>" class="main-photo" alt="<%= member.name %>">
            <div class="bio-info">
                <h1><%= member.name %></h1>
                <p><strong>Birthday:</strong> <%= member.birthday %></p>
                <p><strong>Location:</strong> <%= member.location %></p>
                <p><strong>Known for...:</strong> <%= member.knownFor %></p>
            </div>
        </div>

        <div class="album-grid">
            <% member.album.forEach(photo => { %>
                <div class="album-item">
                    <img src="/familytree/<%= photo.src %>" class="album-photo" onerror="this.src='https://via.placeholder.com/100'">
                    <span class="caption"><%= photo.caption %></span>
                </div>
            <% }) %>
        </div>

        <% if (member.name.includes('Chupp')) { %>
            <div class="special-record-link">
                <h3 style="margin-top: 0;">📜 Family History Found</h3>
                <p>A poem titled <strong>"Doggone Sweet"</strong> was written about this generation in 1940.</p>
                <a href="/familytree/poem" class="btn">Read the Poem</a>
            </div>
        <% } %>

        <a href="/familytree" class="back-btn">← Back to Tree</a>
    </div>

    <div id="imageModal" class="modal">
        <span class="close">&times;</span>
        <div class="modal-content-container">
            <img class="modal-content" id="img01">
            <div id="caption"></div>
        </div>
    </div>

    <script>
        const modal = document.getElementById("imageModal");
        const modalImg = document.getElementById("img01");
        const captionText = document.getElementById("caption");

        document.querySelectorAll('.main-photo, .album-photo').forEach(img => {
            img.style.cursor = "pointer";
            img.onclick = function() {
                modal.style.display = "block";
                modalImg.src = this.src;
                const caption = this.parentElement.querySelector('.caption');
                captionText.innerHTML = caption ? caption.innerHTML : "";
            }
        });

        document.querySelector(".close").onclick = function() { 
            modal.style.display = "none";
        }

        window.onclick = function(event) {
            if (event.target == modal) {
                modal.style.display = "none";
            }
        }
    </script>   
</body>
</html>
